[package]
name = "wintab_lite"
version = "0.1.0"
edition = "2021"

[dependencies]
bitflags = "2.5.0"
libloading = {version = "0.8.3", optional = true}

[dev-dependencies]
libloading = {version = "0.8.3"}


[features]
default=["use_libloading"]
use_libloading = ["libloading"]
